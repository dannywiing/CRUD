# CRUD API

A REST API built using Node.js, Express and MongoDB.
The application provides basic CRUD functionality for managing data through a structured backend system.

## Features
- Create, read, update and delete resources
- MongoDB integration using Mongoose
- Environment variables handled with dotenv
- Structured API with routes and models

## How to run
```bash
npm install
npm start
